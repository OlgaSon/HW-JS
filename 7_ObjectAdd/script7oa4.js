let product = {
    name: 'John',
    price: 30,
    sold: false,
};

let [const1, const2, const3] = Object.keys(product);
console.log(const1, const2, const3)

/* Задача 4
Есть объект. Присвоить каждое его свойство к отдельной переменной.*/