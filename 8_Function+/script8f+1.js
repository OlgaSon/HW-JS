"use strict"

const printMessage = (n) => {
    setInterval(() => alert("message"), n);
};
printMessage(2000);


/* Задача 1.
Напишите функцию printMessage(), которая выводит сообщения на экран каждые n секунд.*/